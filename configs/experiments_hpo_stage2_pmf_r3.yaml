# Stage-2 follow-up round for pmf (generated by scripts/hpo_stage2_round.py from outputs/stage2_pmf, outputs/stage2_pmf_r2):
# one step further out on every axis where the 8-image winner still sat on the edge of everything tried.
runtime:
  seed: 42
  accelerator: auto
  output_root: outputs
  cache_root: cache
  release_model_after_use: true
  continue_on_experiment_error: true
  progress_bars: false
  save_individual_images: true
  verbose: true
  max_atomic_jobs: 4000
  resume: true
  replicate: 0
  gpu_memory_profiling: true
  nvml_sample_interval: 0.02

data:
  source: local_folder
  local_folder: "cache/data/imagenet_val_100"
  image_size: 256

models:
  pmf: {}

defaults:
  control_cost_normalization: sum_squared
  optimizer: adam
  warm_start: false
  grad_clip: null
  record_loss_history: false

metrics:
  lpips: true
  lpips_net: alex

experiments:


  deblurring__dflow__x1:
    enabled: true
    problem: deblur
    num_images: 8
    degradation: {sigma: 0.05, blur_sigma: 1.0, kernel_size: 7, padding: reflect}
    models:
      pmf:
        batch_size: 4
        methods:
          dflow: {t0: 1.0, steps: 1, num_opt_steps: 5120, lr: 0.03}   # edge:num_opt_steps+

  super_resolution__dflow__x1:
    enabled: true
    problem: super_resolution
    num_images: 8
    degradation: {sigma: 0.05, factor: 2}
    models:
      pmf:
        batch_size: 4
        methods:
          dflow: {t0: 1.0, steps: 1, num_opt_steps: 5120, lr: 0.03}   # edge:num_opt_steps+

  random_inpainting__dflow__x1:
    enabled: true
    problem: random_inpaint
    num_images: 8
    degradation: {sigma: 0.01, missing_fraction: 0.70}
    models:
      pmf:
        batch_size: 4
        methods:
          dflow: {t0: 1.0, steps: 1, num_opt_steps: 5120, lr: 0.03}   # edge:num_opt_steps+
